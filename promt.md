
# MASTER PROMPT (Full App Üretmek İçin)
Use the attached RMS_NEXT_CORE_RULES.md, RMS_TAILWIND_TOKEN_CONTRACT.md, RMS_COMPONENT_ARCHITECTURE.md, and RMS_THEME_IMPLEMENTATION.md as strict system contracts.

Generate production-ready Next.js (App Router) + Tailwind components.

Rules:
- Do not use default Tailwind colors.
- Do not hard-code hex values.
- Use semantic token classes only.
- Follow 8px spacing system.
- Ensure enterprise clarity.
- No gradients.
- No startup UI styling.

If uncertain, prioritize stability and readability.
If any default Tailwind color is used, rewrite the component.


-----------------

# new page promt
Using the attached RMS rule files as strict design contracts, generate a production-ready Inventory Management screen.

Requirements:
- Table must support 100+ rows
- Include advanced filters
- Include export button
- Numeric values right-aligned
- Support dense mode
- Must follow all token rules

No hard-coded colors.
No decorative styling.
Enterprise clarity required.

# new component promt

Using the attached RMS rule files as strict design contracts, generate a production-ready Inventory Management screen.

Requirements:
- Table must support 100+ rows
- Include advanced filters
- Include export button
- Numeric values right-aligned
- Support dense mode
- Must follow all token rules
Using RMS rule files as non-negotiable system constraints, generate a reusable ERP-compliant DataTable component.

Must:
- Follow token system
- Support sorting
- Support pagination
- Support dense mode
- Right-align numeric columns
- Include hover and focus states
- Be production-ready

Do not break rule contracts.

No hard-coded colors.
No decorative styling.
Enterprise clarity required.
